# select the sokol-gfx backend depending on platform
if(FIPS_EMSCRIPTEN OR FIPS_RASPBERRYPI OR FIPS_ANDROID)
    add_definitions(-DSOKOL_GLES2)
elseif(FIPS_WINDOWS)
    add_definitions(-DSOKOL_D3D11)
elseif(FIPS_OSX)
    add_definitions(-DSOKOL_METAL)
else()
    add_definitions(-DSOKOL_GLCORE33)
endif()

# a lib of common code shared between all emulators
fips_begin_lib(common)
    fips_vs_warning_level(3)
    fips_dir(common)
    fips_files(gfx.c gfx.h)
    if (FIPS_OSX)
        fips_files(sokol.m)
        if (FIPS_IOS)
            fips_frameworks_osx(UIKit Metal MetalKit)
        else()
            fips_frameworks_osx(Cocoa QuartzCore Metal MetalKit)
        endif()
    else()
        fips_files(sokol.c)
    endif()
fips_end_lib()

fips_begin_app(mz800 windowed)
    fips_vs_warning_level(3)
    fips_files(mz800.c gdg_whid65040_032.h)
    fips_dir(roms)
    fips_generate(FROM mz800-roms.yml TYPE dump SOURCE mz800-roms.c HEADER mz800-roms.h)
    fips_deps(common)
fips_end_app()
